# Dev Log — 2026-04-14

## Session Summary

Full architectural refactoring of DiaryNews based on a system architecture review. The goal was to improve module isolation and expandability by removing the legacy Streamlit frontend, splitting monolithic modules, and introducing proper layering.

---

## Work Done (in order)

### Pre-refactoring: Environment & Config

- Configured tmux mouse scrolling and copy-to-clipboard (`set -g mouse on`, `pbcopy` integration)
- Set up HTTPS for Vite dev server with self-signed cert (accessible on LAN at `https://192.168.31.53:5173`)
- Created Python `.venv` and installed all dependencies
- Fixed uvicorn hot-reload watching `.venv/` — added `reload_excludes=[".venv"]` to `main.py`
- Performed full architectural analysis and scored the codebase (5.5/10 overall, identified 5 key bottlenecks)

### Task 1: Remove Streamlit Frontend

**Deleted:**
- `app.py` (Streamlit entry point)
- `frontend/news.py`, `frontend/youtube.py`, `frontend/__init__.py`
- `frontend/` directory

**Updated:** `CLAUDE.md`, `README.md` to remove all Streamlit references.

**Result:** Zero Streamlit references in active code. Backend was already clean (no Streamlit imports).

### Task 2: Split `storage.py` into Package

**Before:** Single 357-line `backend/storage.py` handling 6 domains.

**After:** `backend/storage/` package with 6 files:

| File | Lines | Responsibility |
|---|---|---|
| `__init__.py` | 18 | Re-exports 14 public functions (backward compatibility) |
| `base.py` | 33 | `_ensure_db()`, `_set_meta()`, `_get_meta()` |
| `news.py` | 66 | News article CRUD |
| `youtube.py` | 145 | Videos, channels, captions CRUD |
| `ideas.py` | 42 | Ideas CRUD |
| `migration.py` | 82 | JSON-to-SQLite migration (startup-only) |

**Key decision:** Lazy import in `_ensure_db()` to avoid circular import between `base.py` and `migration.py`.

**Result:** `api.py` required zero changes — all imports still resolve via `__init__.py` re-exports.

### Task 3: Create Service Layer

**Created:** `backend/services.py` with 6 orchestration functions:

| Function | Extracted from |
|---|---|
| `fetch_and_save_news()` | `api.py` lines 57-63 |
| `add_youtube_channel()` | `api.py` lines 78-90 |
| `resolve_and_save_channel()` | `api.py` lines 101-109 |
| `remove_youtube_channel()` | `api.py` lines 93-98 |
| `fetch_and_save_videos()` | `api.py` lines 116-141 |
| `get_or_fetch_caption()` | `api.py` lines 147-163 |

**Slimmed `api.py`:** Handlers are now thin wrappers — no business logic imports (`fetch_all_feeds`, `fetch_all_channels`, `resolve_youtube_channel`, `normalise_handle` all removed from api.py).

**Design rules enforced:**
- `services.py` has zero FastAPI/starlette imports (verified via AST check)
- All service functions are synchronous; `api.py` wraps with `asyncio.to_thread`
- Domain exceptions (`ValueError`, `KeyError`) translated to `HTTPException` at the API layer

### Task 4: Split Config

**Created:** `backend/sources.py` — domain data (`RSS_SOURCES`, `CATEGORIES`)

**Cleaned:** `backend/config.py` — now infrastructure-only (DB path, API URLs, feature flags)

**Updated imports in:** `backend/news.py`, `backend/api.py`

### Task 5: Extract Prompt Templates

**Created:** `backend/prompts.py` with two functions:
- `article_summary_prompt(title, content)` — Portuguese news summary
- `caption_summary_prompt(title, raw_text)` — Chinese structured video summary

**Updated:** `backend/news.py` and `backend/youtube.py` — removed inline prompt strings, call prompt functions instead. Truncation (`[:3000]`, `[:12000]`) stays at call site.

### Task 6: Update Documentation

**Updated all 3 docs:**
- `CLAUDE.md` — Full architecture with service layer, storage package, sources, prompts
- `README.md` — Updated project structure tree
- `EXPLANATION.md` — Removed all Streamlit/JSON references, describes FastAPI+React+SQLite flow

---

## Final Architecture

```
backend/
  api.py              # Thin FastAPI handlers (HTTP concerns only)
  services.py         # Orchestration layer (fetch -> process -> save)
  config.py           # Infrastructure constants (DB, API URLs, feature flags)
  sources.py          # Domain data (RSS feeds, categories)
  prompts.py          # LLM prompt templates
  llm.py              # MiniMax API wrapper (unchanged)
  news.py             # RSS fetching, scraping, classification (unchanged)
  youtube.py          # YouTube fetching, captions, Whisper (unchanged)
  utils.py            # strip_html (unchanged)
  database.py         # SQLite init + connection (unchanged)
  storage/
    __init__.py       # Re-exports (backward compatibility)
    base.py           # DB init, metadata helpers
    news.py           # News CRUD
    youtube.py        # Videos/channels/captions CRUD
    ideas.py          # Ideas CRUD
    migration.py      # JSON -> SQLite migration (startup-only)

react-frontend/       # React SPA (no changes needed)
main.py               # uvicorn entry point
```

## Architecture Score (post-refactoring estimate)

| Dimension | Before | After |
|---|---|---|
| Backend/Frontend separation | 7/10 | 9/10 |
| Module cohesion | 4/10 | 8/10 |
| Coupling | 5/10 | 7/10 |
| Expandability | 4/10 | 7/10 |
| LLM abstraction | 9/10 | 9/10 |
| Data layer isolation | 6/10 | 8/10 |
| **Overall** | **5.5/10** | **8/10** |

## Files Changed

**Deleted (4):** `app.py`, `frontend/news.py`, `frontend/youtube.py`, `frontend/__init__.py`

**Created (10):**
- `backend/services.py`
- `backend/sources.py`
- `backend/prompts.py`
- `backend/storage/__init__.py`
- `backend/storage/base.py`
- `backend/storage/news.py`
- `backend/storage/youtube.py`
- `backend/storage/ideas.py`
- `backend/storage/migration.py`
- `REFACTORING_PLAN.md`

**Modified (7):**
- `backend/api.py` — slimmed to thin wrappers
- `backend/config.py` — removed domain data
- `backend/news.py` — updated imports (sources, prompts)
- `backend/youtube.py` — updated imports (prompts)
- `main.py` — added `reload_excludes=[".venv"]`
- `CLAUDE.md`, `README.md`, `EXPLANATION.md` — full documentation update
- `~/.tmux.conf` — mouse scroll + pbcopy integration
- `react-frontend/vite.config.js` — HTTPS + host binding
